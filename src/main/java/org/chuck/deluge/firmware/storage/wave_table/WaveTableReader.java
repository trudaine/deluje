package org.chuck.deluge.firmware.storage.wave_table;

import java.io.IOException;

public class WaveTableReader {
  public static void readWavetable(WaveTable waveTable, String path) throws IOException {
    // This is a stub. Real implementation would parse the WAV file and generate bands.
    // For high-fidelity, we should implement the FFT band generation or load pre-generated bands.
  }
}
