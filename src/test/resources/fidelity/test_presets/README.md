# Deluge fidelity test presets (native 2.0.0-beta format)

Generated by `scripts/gen_test_presets.py`. Each preset isolates ONE synthesis feature so a
clean hardware reference can be recorded from it. Built from the exact structure of a real
ludocard preset (proven to load on hardware), overriding only per-test fields — NOT via the
Java serializer (which emits a divergent format the hardware may reject).

## Recording procedure
1. Copy a preset to the Deluge SD card `SYNTHS/` (rename to an unused number if needed).
2. Send **MIDI note 60, velocity 100**, hold >= 3 s, then release.
   (Note 60 matches `FidelityScorecardTest.renderSynth`; the engine renders the same note.)
3. **Resample** on the Deluge (digital output), 44.1 kHz; keep peak around -3 to -6 dB.
4. Align: `python3 scripts/align_recording.py <REC>.WAV src/test/resources/fidelity/<reference>.wav <targetSample>`.

## Presets

| preset | tests |
|---|---|
| `T01_dry_saw.XML` | Dry sawtooth, LPF open, no FX |
| `T02_dry_square.XML` | Dry square (50%), LPF open |
| `T03_dry_sine.XML` | Dry sine |
| `T04_dry_triangle.XML` | Dry triangle |
| `T05_dry_analogsaw.XML` | Dry analog saw (band-limited table) |
| `T06_dry_analogsquare.XML` | Dry analog square |
| `T07_noise.XML` | Pure white noise (osc volume off, noise on) |
| `T08_lpf_saw.XML` | Saw through 24dB LPF (mid cutoff) |
| `T09_lpf_resonant.XML` | Saw, 24dB LPF mid cutoff + high resonance |
| `T10_hpf_saw.XML` | Saw through HPF (mid cutoff) |
| `T11_hpf_resonant.XML` | Saw, HPF mid + high resonance |
| `T12_lpf_12db.XML` | Saw through 12dB LPF (mid cutoff) |
| `T13_pwm_static.XML` | Square with static ~25% pulse width |
| `T14_pwm_lfo.XML` | Square, LFO1 -> pulse width sweep |
| `T15_saw_sync.XML` | Saw osc2 hard-synced to osc1 (osc2 +7 semis) |
| `T16_square_sync.XML` | Square osc2 hard-synced to osc1 |
| `T17_fm_simple.XML` | FM: sine carrier, modulator1 ratio 1 (transpose 0), moderate amount |
| `T18_fm_feedback.XML` | FM: modulator1 ratio 1 + feedback |
| `T19_fm_bell.XML` | FM bell: modulator1 high ratio (transpose +14) |
| `T20_unison_saw.XML` | Saw, 4-voice unison with detune |
| `T21_unison8_saw.XML` | Saw, 8-voice unison, wide detune |
| `T22_lfo_vibrato.XML` | LFO1 -> pitch (vibrato) |
| `T23_lfo_tremolo.XML` | LFO1 -> volume (tremolo) |
| `T24_lfo_lpf.XML` | LFO1 -> LPF frequency |
| `T25_fast_decay.XML` | Saw, fast decay to silence (plucky, sustain 0) |
| `T26_slow_attack.XML` | Saw, slow attack swell |
| `T27_pitch_env.XML` | Envelope2 -> pitch sweep |
| `T28_drive_saturate.XML` | Saw with clippingAmount (saturation/drive) |
